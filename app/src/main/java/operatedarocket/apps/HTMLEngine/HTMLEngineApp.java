package operatedarocket.apps.Folders;

import operatedarocket.ui.AppFrame;

import javax.swing.*;

public class ResoruceFoldersApplication extends AppFrame {
    public ResoruceFoldersApplication(String title, String imgPath) {
        super(title, imgPath);

        setSize(300, 300);
        
    }
}
